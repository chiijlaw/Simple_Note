/*
 * DisplayNotes Messages
 *
 * This contains all the text for the DisplayNotes container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DisplayNotes';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DisplayNotes container!',
  },
});
